<h1>New subcategory</h1>

<%= render 'form'%>

<%= form_for @subcategory, :url => subcategories_path, :html => { :multipart => true } do |form| %>
  <%= form.file_field :picture %>
<input type="submit"/>
<% end %>

<%= link_to 'Back', subcategories_path %>
